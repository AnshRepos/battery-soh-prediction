# Battery SOH Dashboard — Environment Variables
# This project reads data from bundled static assets (no live API).
# If you later add a FastAPI backend, set this URL and update metricsService.js.
VITE_API_BASE_URL=http://localhost:8000
